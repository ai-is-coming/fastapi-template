[DEFAULT]
# Yoyo migrations configuration
sources = migrations
# Database URL - yoyo uses different driver format than SQLAlchemy
# Note: yoyo uses psycopg2 format, not asyncpg
database = postgresql://root:password@127.0.0.1:5432/alice
verbosity = 1
batch_mode = on
